# custom scripts
[[ -d "$HOME/.scripts" ]] && PATH="$HOME/.scripts:$PATH"

# rgrav
[[ -d "$HOME/projects/rgrav" ]] && PATH="$HOME/projects/rgrav:$PATH"

# ruby
[[ -d "$HOME/.rbenv/bin" ]] && PATH="$HOME/.rbenv/bin:$PATH"

# ansible
[[ -d "$HOME/.local/bin" ]] && PATH="$HOME/.local/bin:$PATH"

# vscode on windows
[[ -d /c/Users/alex/AppData/Local/Programs/Microsoft\ VS\ Code/bin ]] && PATH=/c/Users/alex/AppData/Local/Programs/Microsoft\ VS\ Code/bin:$PATH

export PATH
